# TODO

- Enter moved from config to glb
- dodać Enter do wszystkich modeli
- uprościć model. bez user.data tylko typ i nazwa/identyfikator obiektu zakodowane w nazwie, eg: type_name

- Add universal audio trigger zones: allow config `audioZones` to target explicit Blender trigger objects/volumes, not only named floor mesh bounds, so future multi-room scenes can route audio with invisible zone volumes.



